a pyhton script for converting JSON to CSV

script uses the tkinter library:

sudo apt-get install python3-tk


Execute the “JSON to CSV” - ./json_to_csv.py 
The script parses through the JSON file,
reformats it to CSV format, saves the new CSV file to the source folder.


